name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v2

      # Set up Docker Buildx (a tool for building multi-platform images)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Cache Docker layers to speed up the build process
      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # Log into Docker Hub using GitHub Secrets
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      # Build and push the Docker image
      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/payment-api:${{ github.sha }} .
          docker push ${{ secrets.DOCKER_USERNAME }}/payment-api:${{ github.sha }}
          
      # Optional: Tag the image as 'latest'
      - name: Tag image as latest
        run: |
          docker tag ${{ secrets.DOCKER_USERNAME }}/payment-api:${{ github.sha }} ${{ secrets.DOCKER_USERNAME }}/payment-api:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/payment-api:latest
